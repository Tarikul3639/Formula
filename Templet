/*##################################################################
 #                   Bismillahir Rahmanir Rahim                    #
 #                         ALLAHU AKBAR                            #
 #                                                                 #
 #                  ***Author : TARIKUL ISLAM                      #
 #        Bangladesh University of Business and Technology,(BUBT)  #
 #                      ** Dept.of CSE.                            #
###################################################################*/

#include <bits/stdc++.h>
using namespace std;

#define dublicate_alement_remove(vector_name)                   \
    auto last = unique(vector_name.begin(), vector_name.end()); \
    vector_name.erase(last, vector_name.end());

#define freequency_count(mapname)                              \
    string maax;                                               \
    int currentMax = 0;                                        \
    for (auto it = mapname.begin(); it != mapname.end(); it++) \
    {                                                          \
        if (it->second > currentMax)                           \
        {                                                      \
            currentMax = it->second;                           \
            maax = it->first;                                  \
        }                                                      \
    }
#define ll long long
#define nn '\n'
#define EPS 1e-9
#define PI 3.1415926535897932384626433832795
#define pb push_back
#define asort(a) sort(a.begin(), a.end())
#define areverse(a) reverse(a.begin(), a.end())
#define sp " "
#define test  \
    ll t;     \
    cin >> t; \
    while (t--)
int main()
{
  

    return 0;
}
